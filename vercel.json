vercel.json code:

{
    "builds": [{
        "src": "mysite1/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.11" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "mysite1/wsgi.py"
        }
    ]
}
